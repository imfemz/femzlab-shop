# Sortie de build : entièrement reconstruite par `python3 build.py`.
# La versionner ajouterait 17 Mo à l'historique à chaque modification.
dist/

.DS_Store

# secrets locaux de wrangler (webhook Discord)
.dev.vars

# cache local de wrangler pages dev (sqlite miniflare, rien de sensible)
.wrangler/

# artefacts de la suite de tests Python
__pycache__/
